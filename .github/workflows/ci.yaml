# name: Borderless Items Manager CI/CD pipeline

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# env:
#   DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}

# jobs:
#   test-backend:
#     name: Test Backend
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: backend
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: "npm"
#           cache-dependency-path: backend/package-lock.json

#       - name: Install dependencies
#         run: npm ci

#       - name: Run tests
#         run: npm test

#   build-and-push:
#     name: Build & Push Images
#     runs-on: ubuntu-latest
#     needs: test-backend
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'
#     steps:
#       - uses: actions/checkout@v4

#       - name: Log in to DockerHub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Build & push backend
#         uses: docker/build-push-action@v5
#         with:
#           context: ./backend
#           push: true
#           tags: ${{ secrets.DOCKERHUB_USERNAME }}/borderless-items-manager-backend:latest

#       - name: Build & push frontend
#         uses: docker/build-push-action@v5
#         with:
#           context: ./frontend
#           push: true
#           tags: ${{ secrets.DOCKERHUB_USERNAME }}/borderless-items-manager-frontend:latest

#   deploy:
#     name: Deploy to EC2 Minikube
#     runs-on: ubuntu-latest
#     needs: build-and-push
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'
#     steps:
#       - uses: actions/checkout@v4

#       - name: Deploy via SSH
#         uses: appleboy/ssh-action@v1
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ${{ secrets.EC2_USER }}
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             cd ~/borderless-items-manager || git clone https://github.com/${{ github.repository }}.git ~/borderless-items-manager
#             cd ~/borderless-items-manager
#             git pull origin main
#             helm upgrade --install borderless ./helm
#             kubectl rollout restart deployment/borderless-backend -n borderless
#             kubectl rollout restart deployment/borderless-frontend -n borderless
#             kubectl get pods -n borderless
